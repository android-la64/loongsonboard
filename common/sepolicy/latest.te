# latest service

type latest, domain;
type latest_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(latest)
#domain_auto_trans(init, shell_exec, latest)

# XC-TODO, if in system/bin, should: typeattribute latest coredomain;
